bitwarden-sdk-server:
  enabled: true

installCRDs: true

log:
  level: error

serviceMonitor:
  enabled: true

webhook:
  log:
    level: error

certController:
  log:
    level: error

metrics:
  listen:
    port: 8080
  service:
    enabled: true